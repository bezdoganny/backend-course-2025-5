{
  "name": "backend-course-2025-5",
  "version": "1.0.0",
  "description": "Примітивний кешуючий проксі-сервер для http.cat",
  "main": "index.js",
  "scripts": {
    "start": "node index.js --host 127.0.0.1 --port 3000 --cache ./cache",
    "dev": "nodemon --watch . --ext js --exec \"node index.js --host 127.0.0.1 --port 3000 --cache ./cache\""
    
  },
  "dependencies": {
    "commander": "^10.0.0",
    "superagent": "^8.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.0"
  }
}
